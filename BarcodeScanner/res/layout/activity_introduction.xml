<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".IntroductionActivity" >

    <ImageView
        android:id="@+id/logotype_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerInParent="true"
        android:contentDescription="@string/logotype_description"
        android:src="@drawable/ic_launcher" />
    <!-- This should be replaced with the logotype -->

    <TextView
        android:id="@+id/intro_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_below="@id/logotype_image"
        android:text="@string/intro_message" />

    <Button
        android:id="@+id/admin_button"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/customer_button"
        android:layout_alignLeft="@+id/customer_button"
        android:text="@string/admin_button"
        android:onClick="enterAdmin" />

    <Button
        android:id="@+id/customer_button"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:text="@string/customer_button"
        android:onClick="enterCustomer" />

</RelativeLayout>